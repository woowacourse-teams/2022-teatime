import Calendar from '@components/Calendar';
import { ScheduleContainer } from './styles';

const Schedule = () => {
  return (
    <ScheduleContainer>
      <Calendar />
    </ScheduleContainer>
  );
};

export default Schedule;
